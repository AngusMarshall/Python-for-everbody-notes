#This assignments task is to calculate gross pay by calculating hours and rate.
#create and hours variable and make it a floating point this will allow values of 0.0
hrs = float(input("Enter Hours:"))
#create a rate variable and make it a floating point
rate = float(input("Enter Rate:"))
#create a pay variable to calculate the inputted hours and rate
pay = hrs * rate
#print text displaying "Pay" and the calculated variable of pay
print ("Pay:",pay)
